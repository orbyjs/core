/**
 * @property {Object} document - allows to replace the document to be used by default within orby
 * @property {number} delay - controls the microtask waiting time
 */
export let options = {
    delay: 0
};
